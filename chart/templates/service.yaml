apiVersion: v1
kind: Service
metadata:
  name: {{ include "happy-beer-api.fullname" . }}
  labels:
    {{- include "happy-beer-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "happy-beer-api.selectorLabels" . | nindent 4 }}
